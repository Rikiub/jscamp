{
	"name": "project-monorepo",
	"private": true,
	"scripts": {
		"dev": "pnpm -r --parallel dev",
		"format": "pnpm -r exec biome format --write .",
		"lint": "pnpm -r exec biome check .",
		"lint:fix": "pnpm -r exec biome check --write ."
	},
	"devDependencies": {
		"@biomejs/biome": "^2.3.12",
		"typescript": "^5.9.3"
	}
}
